# Habilita estatísticas detalhadas
ExtendedStatus On

<VirtualHost *:80>

    # Status local
    <Location "/server-status">
        SetHandler server-status
        Require local
    </Location>

    # Redireciona restante para HTTPS
    RewriteEngine On
    RedirectMatch ^/(?!server-status$) https://192.168.0.25

</VirtualHost>

<VirtualHost *:443>

    # SSL
    SSLEngine on
    SSLCertificateFile /etc/ssl/certs/wildcard.gru.com.br.cert.pem
    SSLCertificateKeyFile /etc/ssl/certs/wildcard.gru.com.br.key.pem

    SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
    SSLCipherSuite PROFILE=SYSTEM
    SSLHonorCipherOrder on
    SSLSessionTickets off
    SSLCompression off

    # Segurança
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Content-Type-Options nosniff

    # Status local
    <Location "/server-status">
        SetHandler server-status
        Require local
    </Location>

</VirtualHost>

