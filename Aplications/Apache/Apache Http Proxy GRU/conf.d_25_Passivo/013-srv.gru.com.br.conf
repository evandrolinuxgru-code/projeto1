<VirtualHost *:80>

    ServerName srv.gru.com.br
    RewriteEngine On
    RewriteCond %{HTTPS} !=on
    RewriteRule ^/(.*)$ https://%{SERVER_NAME}/$1 [R=301,L]
	
</VirtualHost>

<VirtualHost *:443>

    ServerName srv.gru.com.br

    # SSL
    SSLEngine on
    SSLCertificateFile /etc/ssl/certs/wildcard.gru.com.br.cert.pem
    SSLCertificateKeyFile /etc/ssl/certs/wildcard.gru.com.br.key.pem
    SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
    SSLCipherSuite PROFILE=SYSTEM
    SSLHonorCipherOrder on
    SSLSessionTickets off
    SSLCompression off

    # Segurança
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Content-Type-Options nosniff

    # Configuração do Proxy
	<Proxy balancer://srv_cluster>
	
        BalancerMember http://172.31.50.89/
        ProxySet lbmethod=byrequests
		Require all granted
		
	</Proxy>

    ProxyPass / balancer://srv_cluster/
    ProxyPassReverse / balancer://srv_cluster/
	
    # Logs
    ErrorLog logs/srv_error.log
    CustomLog logs/srv_access.log combined

</VirtualHost>


