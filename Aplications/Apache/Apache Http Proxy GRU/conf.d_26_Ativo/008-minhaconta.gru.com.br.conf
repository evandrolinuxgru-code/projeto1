<VirtualHost *:80>
    ServerName minhaconta.gru.com.br
    RewriteEngine On
    RewriteCond %{HTTPS} !=on
    RewriteRule ^/(.*)$ https://%{SERVER_NAME}/$1 [R=301,L]
</VirtualHost>

<VirtualHost *:443>
    ServerName minhaconta.gru.com.br

    # Certificado do Apache (frontend)
    SSLEngine on
    SSLCertificateFile      /etc/ssl/certs/STAR_gru_com_br.crt
    SSLCertificateKeyFile   /etc/ssl/private/wildcard_gru.key
    SSLCertificateChainFile /etc/ssl/certs/Sectigo_RSA_Domain_Validation.crt

    # Segurança SSL (mantive as suas recomendações, pode ajustar conforme política interna)
    SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
    SSLHonorCipherOrder on
    SSLCompression off
    SSLSessionTickets off

    # Cabeçalhos de segurança
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Content-Type-Options nosniff
    Header always set X-XSS-Protection "1; mode=block"

    # Proxy
    ProxyPreserveHost On
    ProxyRequests Off
    SSLProxyEngine On

    ProxyPass        / https://sprdapadsgru01.gru.com.br:9251/
    ProxyPassReverse / https://sprdapadsgru01.gru.com.br:9251/

    # Logs
    ErrorLog  logs/minhaconta_error.log
    CustomLog logs/minhaconta_access.log combined
</VirtualHost>

